# Outliers

Done by calculating Upper and Lower fence. - usually done by stats software

Upper fence = Q3 + 1.5(IQR)
Lower fence = Q1 - 1.5(IQR)

Values above the upper fence and below the lower fence are defined as outliers.